import { build } from './core/build.core';
import type {
  ChangePackage,
  CopyFiles,
  Options,
  Path,
  SkipDirectory,
  SkipExtension,
  SkipFile,
} from './types';

export type {
  ChangePackage,
  CopyFiles,
  Options,
  Path,
  SkipDirectory,
  SkipExtension,
  SkipFile,
};
export { build };
